name: Test wheel builds
env:
  CLONE_PATH: ${{ github.workspace }}

on:
  push:
    branches: [ main ]

jobs:

  build_wheels:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-2022]
        python-version: ["3.9"]
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Build & install
      shell: bash
      run: |
        uname -a
        python -m pip install --upgrade pip
        python -m pip install --upgrade setuptools
        # This didn't make a difference 
        #python -m pip install build==1.0.0
        python -m pip install build
        pushd $CLONE_PATH
        python -m build --wheel
        python -m pip install dist/mytest-*.whl
        popd
        which python
        python -m pip list
        python -c "import mytest ; mytest.do_something()"
        python -m pip install twine
        twine check dist/mytest-*.whl

    - name: Archive prebuilt wheels
      uses: actions/upload-artifact@v4
      with:
        name: manual-wheels-${{ matrix.os }}
        path: dist/mytest-*.whl
